[project]
name = "accessops-iam-lab"
version = "1.0.0"
description = "Beginner-friendly workforce IAM lifecycle lab using Keycloak, OIDC, Python, and REST/JSON"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
